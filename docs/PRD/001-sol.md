# PRD 001: Sol

**Created:** 2026-02-28
**Updated:** 2026-02-28
**Status:** Active

## 1. Vision

Sol is a mobile-first web application that serves as a full remote frontend to pi (pi.dev), a CLI-based AI coding agent. It runs on the developer's laptop and is accessed from an iPhone via Safari over Tailscale VPN.

Sol enables complete pi-based workflows while away from the keyboard: browsing sessions, reading conversation history, sending prompts, reviewing agent-generated code, inspecting modified files, switching models, and starting new sessions. It replaces the need for a mobile SSH terminal by providing a touch-optimized, polished interface purpose-built for working with pi on a phone.

Named after Sol Robeson, Max's mentor in the film π.

## 2. User Stories

### 2.1 Session Management

- As a developer, I want to see all my pi sessions grouped by project directory so I can find past conversations without remembering file paths.
- As a developer, I want sessions sorted by recency so the most relevant ones appear first.
- As a developer, I want to see session metadata (message count, timestamps, first message preview) so I can identify sessions at a glance.
- As a developer, I want to connect to an active pi session so I can continue working on it from my phone.

### 2.2 Conversation

- As a developer, I want to view a session's full conversation history with proper formatting so I can review what the agent did.
- As a developer, I want user messages, assistant responses, and tool results visually distinguished so the conversation is easy to follow.
- As a developer, I want to expand and collapse tool calls so I can focus on the conversational flow without clutter.
- As a developer, I want to see model info, token counts, and cost per message so I can understand resource usage.
- As a developer, I want to send prompts to the active session with a mobile-friendly input so I can direct the agent from my phone.
- As a developer, I want to see streaming agent responses in real-time so I know what the agent is doing as it works.

### 2.3 Tree Navigation

- As a developer, I want to visualize the conversation tree (branches, forks, compactions) so I can navigate alternate paths the agent explored.
- As a developer, I want to tap a branch point and switch to viewing a different branch so I can compare approaches.

### 2.4 File Inspector

- As a developer, I want to see files the agent has modified or created (git working tree changes) so I can review its work.
- As a developer, I want to view full file contents with syntax highlighting so I can read agent-generated code on my phone.
- As a developer, I want to view markdown files with a toggle between rendered and raw so I can read documentation the agent wrote.
- As a developer, I want to view git diffs for modified files so I can understand what changed.

### 2.5 Model Management

- As a developer, I want to switch the active model mid-session so I can adjust cost/capability tradeoffs from my phone.

### 2.6 Search

- As a developer, I want to search across all sessions by keyword so I can find specific conversations or topics.
- As a developer, I want search results to link directly to the matching session and entry so I can jump to context quickly.

### 2.7 Agent Autonomy & Testing

- As a developer, I want agents to verify their own implementations so that ticket completion doesn't require manual human testing.
- As a developer, I want automated regression detection so that new features don't silently break existing ones.
- As a developer, I want every ticket to include runnable acceptance tests so agents have a clear, machine-verifiable definition of done.

### 2.8 UX Polish — Terminal-Like Aesthetic (Updated: 2026-03-01)

**Goal:** Sol should look and feel like the pi terminal CLI, not like a chat messaging app.

**Core principle:** The pi CLI looks good because it uses **typography and color** to create hierarchy — not nested boxes, bubbles, badges, or heavy borders. Sol must adopt this same approach.

- As a developer, I want agent messages rendered with terminal-style formatting so they match the pi CLI aesthetic and are easy to read.
- As a developer, I want the conversation to flow naturally like a terminal session, not feel like a chat app with heavy bubbles and borders.
- As a developer, I want tool executions (bash, read, write, etc.) rendered **inline** as part of the conversation flow, not as separate disconnected bubbles.
- As a developer, I want minimal visual chrome — no redundant labels, no nested containers, no always-visible buttons cluttering the view.
- As a developer, I want a sleek floating toolbar that I can collapse so session actions don't clutter the reading area.
- As a developer, I want auto-scroll to follow new messages but pause when I scroll up, with a floating control to re-enable it.
- As a developer, I want no messages ever hidden behind the prompt input so I can always see the full conversation.
- As a developer, I want the scroll-to-bottom button visible whenever I'm not at the bottom of a session.

### 2.9 Mobile Experience

- As a developer, I want the UI to feel like a polished native app with snappy navigation so working from my phone doesn't feel like a compromise.
- As a developer, I want a dark OLED-friendly theme that feels like a terminal, not like a messaging app.
- As a developer, I want touch targets that are easy to tap (44pt minimum) without adding visual clutter.

### 2.10 Mobile Composer (Updated: 2026-03-01)

- As a developer, I want a full-screen prompt editor on mobile so I can compose complex prompts without struggling with a tiny input field.
- As a developer, I want to browse and insert prompt templates so I can invoke skills and commands without memorizing syntax.
- As a developer, I want to autocomplete file paths from my project so I can reference files without leaving the composer.
- As a developer, I want to recall previous messages I've sent so I can reuse or modify past prompts.
- As a developer, I want to toggle between the simple input and full composer so I can choose the right tool for the task.

## 3. Functional Requirements

### 3.1 Session List (MVP)

- Discover all sessions via `SessionManager.listAll()` from the pi SDK.
- Group sessions by project directory.
- Sort by last modified descending within each group.
- Display: session name or first message preview, message count, created/modified timestamps.
- Indicate which sessions have an active pi RPC subprocess managed by Sol.

### 3.2 Conversation View (MVP)

- For historical sessions: load entries via `SessionManager.open()` and render the current branch.
- For active sessions: retrieve messages via pi RPC `get_messages` command.
- Render the current branch (root to leaf) as a scrollable conversation view.
- Handle all message types: user, assistant, toolResult, bashExecution, custom, branchSummary, compactionSummary.
- Display assistant metadata: model, provider, token usage, cost.
- Tool calls and results are collapsed by default, expandable on tap.
- Optimized for reading conversational flow between user and assistant.

#### 3.2.1 Visual Design Philosophy — Terminal Aesthetic (Updated: 2026-03-01)

Sol's conversation view must match the **pi CLI terminal aesthetic**, not look like a chat messaging app.

**What makes the pi CLI look good (from screenshot analysis):**

```
Now let me run the build to verify everything compiles:    ← Agent thinking (muted italic)

Now let's verify the build:                                ← Agent text (primary)

$ cd /Users/.../sol && npm run build 2>&1                  ← $ accent, command bold
> sol@0.1.0 build                                          ← Output muted
> tsc --noEmit && cd frontend && npx tsc --noEmit          ← Output continues

Build passes. Let me run the tests:                        ← Agent text resumes
```

**Key characteristics:**
1. **Single unified background** — no nested containers, boxes, or bubbles
2. **Typography-driven hierarchy** — bold, italic, color, font-size (NOT backgrounds/borders)
3. **Inline tool rendering** — tools flow naturally within the conversation, not separate widgets
4. **Minimal labels** — `$` prefix is enough for bash, no "AGENT" or "bash" labels needed
5. **Color hierarchy** — muted for thinking, primary for prose, accent for special chars, muted for output
6. **Monospace for code** — bash commands and output in monospace
7. **Compact spacing** — information-dense, not padding-heavy
8. **Hidden controls** — Copy/expand controls appear on interaction, not always visible

**What Sol currently does wrong:**
1. Heavy "chat bubble" containers with thick colored borders
2. Nested boxes (bg-surface-2 inside bg-surface)
3. Redundant labels ("AGENT", "bash" badge, then "bash" label again)
4. Tool results as separate disconnected bubbles below the agent message
5. Always-visible "Copy" and "Less" buttons adding clutter
6. Too much padding and rounded corners — feels like a messaging app

**Required changes:**
1. **Remove chat bubbles** — use typography/color hierarchy instead
2. **Inline tool results** — part of the message flow, not separate containers
3. **Minimal role indication** — subtle left accent line or small muted label, not heavy borders
4. **User messages** — subtle visual distinction (slight indent or background tint) without heavy bubbles
5. **Controls on demand** — Copy/expand via long-press or tap, not always visible
6. **Tighter spacing** — reduce padding, increase information density

#### 3.2.2 Tool Rendering (Updated: 2026-03-01)

All tool results must render **inline** within the conversation flow, matching the pi CLI aesthetic.

**Inline rendering pattern:**

```
{agent prose text}

$ {bash command}                              ← $ in accent, command in primary/bold
{output line 1}                               ← Output in muted, monospace
{output line 2}
... (N more lines — tap to expand)            ← Truncation hint in accent

{agent prose continues}
```

**Per-tool formatting:**

| Tool | Header Format | Preview Lines | Special Handling |
|------|---------------|---------------|------------------|
| `bash` | `$ {command}` | 5 | Exit code badge if non-zero |
| `read` | `read {path}` | 10 | Syntax highlighting if possible |
| `write` | `write {path}` | 10 | Syntax highlighting, line count |
| `edit` | `edit {path}` | Full diff | Diff with +/- coloring |
| `ls` | `ls {path}` | 20 | Directory listing |
| `find` | `find {pattern}` | 20 | File list |
| `grep` | `grep /{pattern}/` | 15 | Match highlighting |

**No nested boxes** — command and output are styled text, not containers within containers.

**Per-Tool Rendering:**

| Tool | Header Format | Preview Lines | Special Handling |
|------|---------------|---------------|------------------|
| `bash` | `$ {command}` | 5 | Exit code badge, cancelled/truncated indicators |
| `read` | `read {path}:lines` | 10 | Syntax highlighting if possible |
| `write` | `write {path}` | 10 | Syntax highlighting, line count |
| `edit` | `edit {path}:line` | Full diff | Diff with +/- coloring |
| `ls` | `ls {path}` | 20 | Directory listing style |
| `find` | `find {pattern} in {path}` | 20 | File list |
| `grep` | `grep /{pattern}/ in {path}` | 15 | Match highlighting |

**Generic Fallback:** Unknown tools show tool name + content preview.

**Data Limitation:** `toolResult` entries contain only the result, NOT the original arguments (file path, command, pattern, etc.). Sol must:
1. Parse context from result content where possible (e.g., "Successfully replaced text in {path}")
2. Show useful metadata (line counts, result counts) as fallback
3. Display just the tool name when no context can be extracted

| Tool | What We Have | What We Show |
|------|--------------|--------------|
| `bash` | Output only, no command | `bash` + first line preview |
| `read` | File contents, no path | `read` + line count |
| `write` | Success message with path | Path extracted from message |
| `edit` | Success message with path | Path extracted from message |
| `ls` | Directory listing, no path | `ls` + item count |
| `find` | Results, no pattern | `find` + result count |
| `grep` | Matches, no pattern | `grep` + match count |

#### 3.2.3 Text Processing

**ANSI Escape Sequences:** Terminal output often contains ANSI escape codes for colors and formatting. Sol must strip these codes before rendering — they should not appear as raw escape sequences in the UI. Use a utility function to remove CSI sequences, OSC sequences, and simple escape sequences.

**Overflow Handling:** The page must never scroll horizontally. Long content must be handled properly:

1. **Prose text** (markdown, messages): Use `word-wrap: break-word` and `overflow-wrap: break-word` to wrap at word boundaries.
2. **Code/terminal output**: Display in scrollable containers with `overflow-x: auto`. The container stays within viewport bounds while content scrolls horizontally.
3. **Commands and paths**: May scroll horizontally within their header container.

**Key principle:** Users must always be able to view full content — either by wrapping (for prose) or by scrolling within a bounded container (for code/output).

**Thinking Text:** Agent thinking should render inline as muted italic text (matching pi CLI), not hidden in a collapsible by default. Long thinking blocks (>200 chars or >3 lines) may be collapsible with a preview.

#### 3.2.4 Labels and Chrome

**Minimal labels:** The pi CLI doesn't show "AGENT" or role labels — the conversation flow is self-evident. Sol should follow this pattern:
- No role labels by default
- User messages distinguished by subtle background tint
- Agent messages flow naturally without labels
- Tool executions show the tool name and command prominently
- Labels only for special cases (system messages, compaction summaries)

### 3.3 Prompt Input (MVP)

- Mobile-friendly text input with iOS autocorrect and dictation support.
- Send prompts to active session via pi RPC `prompt` command.
- Visual feedback for send state (sending, sent, error).
- Support for steering (`steer`) and follow-up (`follow_up`) RPC delivery modes.

### 3.4 Streaming Output (MVP)

- Sol spawns `pi --mode rpc` subprocesses for active sessions.
- Pi RPC `message_update` events (JSON lines on stdout) are forwarded to the browser via SSE.
- Tokens rendered as they arrive with <50ms perceived latency.
- Tool execution status visible via `tool_execution_start/update/end` events.

### 3.5 File Inspector (MVP)

- List files from git working tree changes (modified, added, untracked) via `git status` in the session's `cwd`.
- View full file contents with syntax highlighting (Shiki) via direct `fs` read.
- View markdown files with toggle between rendered and raw. Rendered view uses typographic prose styling (heading hierarchy, list indentation, blockquote accents, code block formatting, table borders) tuned to dark theme design tokens for comfortable mobile reading.
- View git diffs in line-by-line mode (diff2html) via `git diff` in the session's `cwd`.

### 3.6 Model Switching (MVP)

- List available models via pi RPC `get_available_models` command.
- Switch model via pi RPC `set_model` command.

### 3.7 Tree Navigation (MVP)

- For historical sessions: build tree via `SessionManager.open()` and `sm.getTree()`.
- For active sessions: retrieve entries via RPC `get_messages` and build tree client-side, or read the session file directly (pi appends are safe to read concurrently).
- Identify branch points (entries with multiple children).
- Breadcrumb-style navigation to switch between branches.
- Show compaction and branch summary entries at their tree positions.

### 3.8 Search (MVP)

- Full-text search across all sessions using `allMessagesText` from `SessionInfo` for fast pre-filtering.
- Within-session search scanning individual entries server-side.
- Return matching sessions with hit count and preview snippets.

### 3.9 Testing & Agent Verification (MVP)

- Vitest as the sole test runner, configured for ESM + TypeScript strict mode.
- Unit tests for all backend modules containing extractable logic.
- API integration tests for every Express endpoint using `supertest`.
- Co-located test files: `src/foo.test.ts` alongside `src/foo.ts`.
- Shared test utilities and mock factories in `src/__test__/`.
- NPM scripts: `npm test` (run all), `npm run test:watch`, `npm run test:coverage`.
- Every implementation ticket requires tests that verify its acceptance criteria.
- Agents must run `npm run build && npm test` before marking any ticket complete.

### 3.10 API

Sol's Express server exposes REST + SSE endpoints. Active session endpoints proxy to `pi --mode rpc` subprocesses. Historical and file endpoints use the SDK and filesystem directly.

### 3.11 Mobile Composer (Updated: 2026-03-01)

The Mobile Composer provides an enhanced prompt editing experience on mobile devices.

**Two-tier input system:**
- **Simple input:** Auto-resizing textarea at the bottom of the session view for quick prompts
- **Full composer:** Full-screen overlay for complex prompts, accessible via expand button

**Composer features:**

| Feature | Trigger | Data Source | Insertion Behavior |
|---------|---------|-------------|-------------------|
| Template insertion | Toolbar button | `get_commands` RPC | Insert skill/command at cursor |
| File completion | Toolbar button | Git-tracked files from `/api/files/:id/tree` | Insert file path at cursor |
| History recall | Toolbar button | User messages in current session | Replace or append to text |

**Toolbar actions:**
- **Templates:** Opens bottom sheet with searchable list of skills and slash commands from pi
- **Files:** Opens bottom sheet with searchable project file tree
- **History:** Opens bottom sheet with previous user messages (most recent first)
- **Send:** Submits prompt via existing RPC prompt command
- **Close:** Returns to session view with inline input

**Keyboard optimization:**
- Full-screen overlay avoids keyboard competition issues
- `inputmode="text"` preserves autocorrect and dictation
- Send button remains accessible above keyboard

**Text transfer:**
- Opening composer transfers text from inline input
- Closing composer (without sending) transfers text back to inline input
- Sending from either mode clears both inputs

See PRD Section 3.10 for endpoint table.

| Endpoint | Method | Backend | Description |
|----------|--------|---------|-------------|
| `/api/sessions` | GET | SDK `SessionManager.listAll()` | Session list with metadata, grouped by project |
| `/api/session/:id` | GET | SDK `SessionManager.open()` | Full entries for one session (historical) |
| `/api/session/:id/connect` | POST | Spawn `pi --mode rpc` | Attach to session, returns RPC process handle |
| `/api/session/:id/stream` | GET (SSE) | RPC stdout → SSE | Stream agent events for active session |
| `/api/session/:id/prompt` | POST | RPC `prompt` command | Send prompt to active session |
| `/api/session/:id/steer` | POST | RPC `steer` command | Steer active session |
| `/api/session/:id/abort` | POST | RPC `abort` command | Abort current operation |
| `/api/session/:id/model` | PUT | RPC `set_model` command | Switch model for active session |
| `/api/session/:id/models` | GET | RPC `get_available_models` | List available models |
| `/api/session/:id/state` | GET | RPC `get_state` | Session state (streaming, model, etc.) |
| `/api/session/:id/commands` | GET | RPC `get_commands` | Available slash commands and skills |
| `/api/tree/:id` | GET | SDK `sm.getTree()` | Tree structure with children |
| `/api/files/:id` | GET | `git status` via `child_process` | Git working tree changes for session's cwd |
| `/api/files/:id/content` | GET | `fs.readFile` | Full file contents |
| `/api/files/:id/diff` | GET | `git diff` via `child_process` | Git diff for a file |
| `/api/sessions/search` | GET | SDK `SessionManager.listAll()` + filter | Search across sessions |
| `/api/session/:id/search` | GET | SDK `SessionManager.open()` + scan | Search within a session |

## 4. Non-Functional Requirements

### 4.1 Performance

- Streaming latency: <50ms time-to-first-token visible on screen after the server emits it.
- Navigation between views: <100ms transition, no perceptible jank.
- Session list load: <200ms for ~50 sessions.
- Production JS bundle: <60KB min+gz (excluding lazy-loaded Shiki WASM).

### 4.2 Security

- No application-level authentication. Tailscale provides network-level security.
- Server binds to `0.0.0.0` for Tailscale access.

### 4.3 Design

- Dark theme with WCAG AA contrast ratios (4.5:1 body text, 3:1 UI components).
- Minimum touch targets: 44×44pt (Apple HIG).
- Consistent spacing on 4px base grid.
- Mobile-first responsive design; primary target is iPhone Safari.
- The app should feel like a polished native app, not a developer tool.

### 4.4 Technology

- Frontend: Preact + Tailwind CSS v4 + Headless UI, built with Vite.
- Backend: Express + TypeScript, run via `tsx`.
- Active session integration: `pi --mode rpc` subprocesses managed by Sol.
- Session discovery: `@mariozechner/pi-coding-agent` SDK (`SessionManager`).
- Syntax highlighting: Shiki (lazy-loaded grammars).
- Markdown rendering: marked.
- Diff rendering: diff2html (line-by-line mode).
- Streaming: SSE (Server-Sent Events), bridging pi RPC stdout to browser.
- Server port: 8081.

See [Frontend Technology Prestudy](../prestudy-frontend-technology.md) for frontend rationale.
See [ADR 005: Pi RPC Integration](../ADR/005-pi-rpc-integration.md) for active session architecture.

## 5. Future Considerations (Backlog)

- Full project file tree browser (beyond git working tree).
- Autocomplete / command palette for slash commands, skills, `@` completions.
- Starting new pi sessions from Sol (project directory picker, model selection).
- Session tagging and favoriting.
- Export sessions as markdown or HTML.
- Side-by-side branch comparison.
- Inline `@` completions with mobile-friendly autocomplete web component.
- WebSocket for live updates when a session is being actively written to.
